screening:
  indicators:
    macd:
      short_period: 6
      long_period: 13
      signal_period: 5
    stochastic:
      k_period: 9
      smooth_k: 3
      d_period: 3
      oversold_threshold: 20
  proximity:
    enable_macd_kd: true
    window_days: 1
  performance:
    # 並列処理の設定
    use_parallel: true  # 並列処理を使用するか（true/false）
    max_workers: null  # 最大スレッド数（nullの場合はCPUコア数を使用）

